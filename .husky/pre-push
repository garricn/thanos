#!/usr/bin/env bash
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-push checks..."

# Set environment variables for non-interactive environment
export CI=true
export FORCE_COLOR=1

# Run our local CI script
./scripts/local-ci.sh 
