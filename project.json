{
  "name": "thanos",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": ".",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "options": {
        "skip": true
      }
    },
    "test": {
      "executor": "nx:noop",
      "options": {
        "skip": true
      }
    },
    "e2e": {
      "executor": "nx:noop",
      "options": {
        "skip": true
      }
    },
    "lint": {
      "executor": "nx:noop",
      "options": {
        "skip": true
      }
    },
    "format": {
      "executor": "nx:noop",
      "options": {
        "skip": true
      }
    }
  },
  "tags": []
}
